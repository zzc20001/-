#ifndef COMMON_H
#define COMMON_H

#include <iostream>
#include <iomanip>        // 用于格式化输出
#include <sstream>        // 用于字符串流
#include <memory>         // 用于智能指针
#include <string>         // 用于字符串操作
#include <stdexcept>      // 用于异常处理
#include <vector>         // 用于动态数组
#include <map>            // 用于键值对容器
#include <set>            // 用于集合
#include <algorithm>      // 用于通用算法
#include <openssl/sha.h>  // 用于 SHA 哈希函数

// 添加其他你需要的头文件

#endif // COMMON_H
